package main

import (
    "fmt"
    //"math/rand"
	"github.com/notnil/chess"
)



func main() {
    // Initialize the population of neural networks
    

    // Have the population all play a number of games against each other in a huge round robin tournament. 


    // Sort the population by score

    // Then, we select the top 20% of the population to breed perfectly, with no lost genetic mateiral
    // Breed 20% of total population size this way
    
    // Next, we breed a random parent from the top 20%, with a random parent from the second best 20%

    // Replace the old population with the new generation
    // Initialize chess board
}
